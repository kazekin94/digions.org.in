apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.appName }}-deployment-v2
  labels:
    app: {{ .Values.appName }}
    version: v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{ .Values.appName }}
      version: v2
  template:
    metadata:
      labels:
        app: {{ .Values.appName }}
        version: v2
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                - key: type
                  operator: In
                  values:
                  - compute
      containers:
      - name: apache
        image: httpd:2.4
        ports:
        - containerPort: 80
