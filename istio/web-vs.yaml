apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: webserver-vs
spec:
  hosts:
  - webserver
  http:
    route:
    - destination:
        host: webserver
        subset: v1